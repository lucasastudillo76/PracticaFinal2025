@model List<ProyectoCoop1._0.Models.Turno>

@{
    ViewData["Title"] = "Turnos Suspendidos";
}

<h2>Turnos Suspendidos</h2>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Fecha</th>
            <th>Servicio</th>
            <th>Tarea</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @if (!Model.Any())
        {
            <tr>
                <td colspan="4" class="text-center">No hay turnos suspendidos o cancelados.</td>
            </tr>
        }
        else
        {
            foreach (var turno in Model)
            {
                <tr>
                    <td>@turno.FechaHora.ToString("dd/MM/yyyy HH:mm")</td>
                    <td>@turno.servicio</td>
                    <td>@turno.tarea</td>
                    <td>
                        @if (turno.estado == "Suspendido")
                        {
                            <a asp-action="Reprogramar" asp-route-id="@turno.id" class="btn btn-sm btn-primary">Reprogramar</a>
                        }
                    </td>
                </tr>
            }
        }
    </tbody>
</table>
